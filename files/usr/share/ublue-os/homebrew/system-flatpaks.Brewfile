# Intentionally empty: bluespin preinstalls its system flatpaks via
# /usr/share/flatpak/preinstall.d (see the repo README), so there is nothing
# for `ujust install-system-flatpaks` to add. This stub keeps that command a
# working no-op and masks the Bluefin base's version of this file.
