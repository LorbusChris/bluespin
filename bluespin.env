# Repo-level build configuration, loaded by the Justfile (and overridable via
# the environment, which is how CI injects the per-variant IMAGE_NAME).
IMAGE_NAME=bluespin
REPO_ORGANIZATION=LorbusChris
REPO_NAME=bluespin
IMAGE_DESC=Bluespin
IMAGE_KEYWORDS=bootc,ublue,universal-blue,bluefin,dx,surface
IMAGE_LOGO_URL=https://avatars.githubusercontent.com/LorbusChris
# Every platform (bluespin, bluespin-dx, bluespin-surface) is built per Fedora
# branch; the branch is the image tag, and `latest` is an alias for the default
# branch. Each branch names its base here, passed to the Containerfile as
# BASE_IMAGE by `just build <platform> <branch>`. These are the only copies of
# the pins; Renovate tracks every *_IMAGE= line in this file (regex manager in
# .github/renovate.json5).
DEFAULT_FEDORA_BRANCH=44
# 44: Bluefin, which Universal Blue publishes for this branch only. bluefin,
# not bluefin-dx -- the developer layer is re-created for the dx platform in
# build_files/dx.sh, so the other images do not carry it.
FEDORA_44_BASE_IMAGE=ghcr.io/ublue-os/bluefin:44@sha256:4c07bfdb3e50d706b7892485cd1324e1010ff7f6e977bcf0de347995d184aa0b
# 45 and rawhide: Fedora's own Silverblue, there being no Bluefin above 44.
# build.sh supplies what a Bluefin base would have shipped. quay tags these
# with expires-after=4w and rebuilds them less often than the 44 images, so
# these digests go stale faster; today the two tags are the same image, and
# they diverge once quay builds a branched 45.
FEDORA_45_BASE_IMAGE=quay.io/fedora-ostree-desktops/silverblue:45@sha256:8ffc09536aa80f0abac8319b044ac1e2da22199c1276d759fdee092b13ca07c6
FEDORA_RAWHIDE_BASE_IMAGE=quay.io/fedora-ostree-desktops/silverblue:rawhide@sha256:8ffc09536aa80f0abac8319b044ac1e2da22199c1276d759fdee092b13ca07c6
CHUNKAH_IMAGE=quay.io/coreos/chunkah:latest@sha256:ff8b8b466a942ec6000445d4001fc661e2fc5a952ad9ee29b4de9ab09d1d1708
